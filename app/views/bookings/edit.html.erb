<div class="container wrapper-blue">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4 new_boat">
      <%= simple_form_for(@booking) do |f| %>
          <%= f.input :start_date %>
          <%= f.input :end_date %>
          <%= f.button :submit, class: 'btn btn-primary'%>
      <% end %>
    </div>
  </div>
</div>
